#!/bin/bash

OLD_VER=$(grep -oE '[0-9]+\.[0-9]+' places.php | head -1)
NEW_VER="1.$(date +%y%m%d%H%M)"
sed -i '' "s/Version: $OLD_VER/Version: $NEW_VER/" places.php

slug="places"
rm -rf build/*
mkdir -p build
# TODO exclude test files
zip -r "build/$slug.zip" \
    includes/ \
    places.php